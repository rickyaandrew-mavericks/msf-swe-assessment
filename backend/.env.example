# Application
NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:3000
UPLOAD_DIR=./uploads

# PostgreSQL — must match values in root .env (used by docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
